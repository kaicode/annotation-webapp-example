Annotation Webapp Example
=========================

An example Java web application using annotation mapping and no web.xml

Build and run with:
```mvn tomcat7:run```

then browse to:
http://localhost:8080/annotation-webapp-example/
